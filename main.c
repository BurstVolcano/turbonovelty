#include <stdio.h>

int main(void)
{
   /* git version is checked by git --version
    * git clone <somelink> is used for cloning a repository on our local machine
    *cd is used to change directory
    *If a file is edited, it is modified,
    *If a file is added, it is untracked
    *If a file is added, it is staged
    *If a file is comitted, it is unchanged
    *
    *git add <filename> is used to stage
    *if there are lots of files, use git add .
    *git comit -m "Some comment" is used to commit
    *
    *git push origin main is used to copy the code written on local computer into git repository
    */
}
